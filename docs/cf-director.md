# The bundle charm

## index

  ```
  {release-number}/
                   {*arch}/
                           {*job-name}.tar.gz
                           index.json # job delinated w/ links
   ```


### index.json
 ```
 {'jobs':[['filename','sha1']]}
 ```
